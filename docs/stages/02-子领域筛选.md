# Stage 02 — 子领域分类筛选（Phase 2）

> 日期：2026-09-11
> 目标：前端加「8 子领域 chips 筛选」——点某个子领域，页面只显示属于该子领域的内容。
>
> 范围说明：本阶段**只做这一件事**。「日报/周报切换」挪到 Phase 3（周报做完后一起上，避免返工）。

---

## 1. 改了什么（总览）

| 文件 | 改动 |
| --- | --- |
| `src/notifier/web.py` | `render_site()` 新增 `subfields` 参数，传给模板 `subfields` 列表 + `field_labels` 映射 |
| `src/main.py` | `run_render_cmd()` 把 `config.subfields` 透传给 `render_site` |
| `templates/index.html.j2` | 新增子领域 chips 导航 + 卡片 `data-field` 属性 + 子领域徽章 + 筛选 JS |
| `tests/test_notifier_web.py` | 新增 1 个测试（chips + data-field + field 徽章渲染） |

---

## 2. 逐个文件详解

### 2.1 `src/notifier/web.py`

`render_site()` 增加参数：
```python
subfields: list[dict[str, str]] | None = None,
```

渲染时给模板传两组数据：
```python
subfields=subfields or [],                       # 渲染 chips 用（key + label 列表）
field_labels={s["key"]: s["label"] for s in (subfields or [])},   # 卡片徽章查 label 用
```

`field_labels` 是个 dict（`{key: label}`），模板里用 `field_labels[a.score.field]` 把 key 转成中文名。

### 2.2 `src/main.py`

`run_render_cmd()` 调用处加一行，把 Phase 1 解析好的 `config.subfields` 传下去：
```python
subfields=config.subfields,
```

### 2.3 `templates/index.html.j2`

**① CSS**：新增 `.field-btn`（chips 按钮）和 `.field`（卡片上的子领域徽章）样式。

**② 卡片宏**：
- `<article ... data-field="{{ a.score.field or '' }}">` —— 每张卡片带上自己的子领域 key，供 JS 筛选。
- 徽章区加一个紫色子领域徽章：
  ```jinja
  {% if a.score.field and field_labels.get(a.score.field) %}
  <span class="field" title="子领域">{{ field_labels[a.score.field] }}</span>
  {% endif %}
  ```

**③ chips 导航**（在「目录」nav 下面新增一个「子领域」nav）：
```jinja
<nav class="toc subfields" id="subfield-nav">
  <h2>子领域 <span class="n" id="field-count-label">（今日）</span></h2>
  <div class="chips" id="field-chips">
    <button class="field-btn active" data-field="" onclick="filterField('')">全部 <span class="n"><b class="n-count">0</b></span></button>
    {% for sf in subfields %}
    <button class="field-btn" data-field="{{ sf.key }}" onclick="filterField('{{ sf.key }}')">{{ sf.label }} <span class="n"><b class="n-count">0</b></span></button>
    {% endfor %}
  </div>
</nav>
```

**④ 筛选 JS**（纯内联，零构建）：
- `filterField(field)`：记录当前 field、更新 chips 高亮、调 `applyFieldFilter()` + `refreshFavoritesSection()`。
- `applyFieldFilter()`：
  - 遍历所有卡片，`data-field` 不匹配的 `display:none`；
  - 归档里没可见卡片的 `<details>` 日期组也隐藏；
  - 「今日/归档」栏若无可见卡片，显示「该子领域暂无匹配内容」的空态提示。
- `refreshFavoritesSection()` 增加一行 `if (card.style.display === 'none') return;`，让「我的收藏」也遵循当前筛选。

**⑤ 条数徽章（跟随当前 tab）**：
- 每个 chip 显示**一个数**，跟随当前激活的 tab：点「今日」显示今日数量、点「归档」显示归档数量、点「收藏」显示收藏数量。
- 全局变量 `currentBand` 记录当前 tab；`switchTab()` 切 tab 时更新它并调 `refreshFieldCounts()`。
- `refreshFieldCounts()` 只统计 `currentBand` 对应栏的卡片，并同步更新 nav 标题上的「（今日）/（归档）/（收藏）」标签。
- `applyFieldFilter()` 里同步更新「今日新增 / 归档」标题上的计数 `(N)`。

---

## 3. 验证结果（已完成 ✅）

| 步骤 | 结果 |
| --- | --- |
| 测试 | **91 passed**（90 + 新增 1 个） |
| 渲染 | `rendered=20`（今日 10 + 归档 10），`site/index.html` |
| HTML 检查 | `field-chips` nav 存在、`filterField` JS 存在、8 个 chips 齐全、卡片带 `data-field` |
| 浏览器实测 | 点「蛋白质/结构」chip 后，20 张卡片正确筛选成 2 张 protein 卡片，chip 高亮 |
| 计数徽章 | 每个 chip 显示一个数，跟随当前 tab（今日/归档/收藏），标题标签与栏计数同步更新 |

---

## 4. 备忘

- 筛选是**纯客户端**行为（内联 JS 改 `display`），不重新请求数据、不影响 SQLite 和渲染结果。
- 子领域徽章、chips 都依赖 Phase 1 的 `field` 数据（scorer 输出、存 `summaries.field`）；`field` 为空的内容点「全部」可见，点具体子领域时会被隐藏。
- 未做「日报/周报切换」——那是 Phase 3。

---

## 5. 遇到的问题与解决（踩坑记录）

### 5.1 切换领域时「条数不变」
- **现象**：点某个子领域 chip，卡片确实筛选了，但 chips 和标题上的数字没变。
- **原因**：那些数字是 Jinja 在**服务端渲染时写死的静态值**（`{{ today|length }}`），而筛选是纯客户端 JS，数字自然不更新。
- **解决**：改成 JS 动态统计——`refreshFieldCounts()` 在页面加载时数卡片数量填充到 chips；`applyFieldFilter()` 每次筛选时更新标题计数。

### 5.2 归档计数一直不更新（`getElementById('archive')` 取到 null）
- **现象**：筛选后「今日新增」计数更新了，「归档」计数纹丝不动。
- **原因**：归档 `<section>` 只有 `data-band="archive"`，**没有 `id="archive"`**；JS 里 `document.getElementById('archive')` 返回 `null`，代码 `if (!sec) return;` 直接跳过，归档计数和空态提示都没执行。
- **解决**：给归档 section 补上 `id="archive"`。教训：JS 用 id 取元素前，先确认模板里该元素确实有 id。

### 5.3 子领域数量「合并」了（今日 + 归档混在一起）
- **现象**：chip 上的数字是「今日新增 + 归档」的**总数**，看不出今天和历史的分别。
- **原因**：最初 `refreshFieldCounts()` 只按 `data-field` 统计全部卡片、不区分所属栏。
- **解决（中间态）**：改为按栏分别统计，每个 chip 显示两个数 `今日 X · 归档 Y`。但随后发现两个数信息冗余（见 5.4），最终改成跟随当前 tab 的单数。

### 5.4 chip 同时显示「今日 + 归档」两个数，信息冗余
- **现象**：每个 chip 同时显示「今日 X · 归档 Y」两个数，用户希望更聚焦——点「今日」tab 只看今日数量、点「归档」tab 只看归档数量。
- **解决**：引入全局变量 `currentBand`，`switchTab()` 切 tab 时更新它并重算 chips；`refreshFieldCounts()` 改为只统计当前激活 band，nav 标题标签同步显示「（今日）/（归档）/（收藏）」。
